# Rails 7.2 to 8.0 Upgrade

This document tracks the changes made during the upgrade of the application template from Rails 7.2 to Rails 8.0.

The command `bin/rails app:update` was run to generate the standard Rails 8 configuration and structure. The following files were either modified (overwritten) or newly created.

## Template Testing
Verified template install with the following:
1. Setup a new local git repo
2. nava-platform app install --template-uri local/path/to/template-application-rails --version branch-name . rails-demo-1
3. Run make .env, make build, make precompile-assets, make lint, and make test
4. Run make start-container, and navigated to localhost:3000 in browser and successfully logged in.

## Updating an existing nava-platform app to use new template version
1. Updated [OSCER](https://github.com/navapbc/oscer) to Rails 8.0, passed all tests, including multiple end-to-end tests.
2. Updated validates_timeliness gem to 8.0 for compatibility with Rails 8.0, as Strata Gem defines 7.0 for compatibility with Rails 7.2.

## Overwritten Files

The following files existed in Rails 7.2 and were updated by the Rails 8.0 generator.

### `bin/rails`
- Reverted changes to keep freeze string literal: true

### `bin/rake`
- Reverted changes to keep freeze string literal: true

### `bin/rubocop`
- Reverted changes which would have removed

```
# frozen_string_literal: true

#
# This file was generated by Bundler.
#
# The application 'rubocop' is installed as part of a gem, and
# this file is here to facilitate running it.
#

ENV["BUNDLE_GEMFILE"] ||= File.expand_path("../Gemfile", __dir__)

bundle_binstub = File.expand_path("bundle", __dir__)

if File.file?(bundle_binstub)
  if File.read(bundle_binstub, 300).include?("This file was generated by Bundler")
    load(bundle_binstub)
  else
    abort("Your `bin/bundle` was not generated by Bundler, so this binstub cannot run.
Replace `bin/bundle` by running `bundle binstubs bundler --force`, then run this command again.")
  end
end
```

### `bin/setup`
- Reverted changes to keep system! "gem install bundler --conservative"
- Reverted changes to keep exec "bin/dev" which uses overman(foreman alternative) for procfile based management. instead of bin/rails restart.

### `config/application.rb`
- Reverted changes to keep internationalization (i18n) settings
- Reverted changes to keep authentication flow (auth_adapter)
- Reverted changes to keep factory_bot suffix
- Reverted changes to keep UUID generation for primary key
- Reverted changes to keep Pundit rescue_responses
- Reverted changes to keepautoload_lib ignore list
- Reverted changes to keep form_with error proc


### `config/boot.rb`
- Reverted changes to keep freeze string literal: true

### `config/environment.rb`
- Reverted changes to keep freeze string literal: true

### `config/initializers/assets.rb`
- Reverted changes to keep additional fontawesome precompiled assets

### `config/initializers/content_security_policy.rb`
- Reverted changes to keep freeze string literal: true

### `config/initializers/filter_parameter_logging.rb`
- Added :cvv, :cvc to filter parameters list

### `config/initializers/inflections.rb`
- Reverted changes to keep freeze string literal: true

### `public/404.html`
- Updated default rails error page

### `public/406-unsupported-browser.html`
- Updated default rails error page

### `public/422.html`
- Updated default rails error page

### `public/500.html`
- Updated default rails error page

### `bin/dev`
- Discarded in favor of dev.jinja which uses overman(foreman alternative) for procfile based management.

### `config/environments/development.rb`
- Discarded in favor of development.jinja
- Keep custom config for default url options
- New config for query_log_tags commented out.

### `config/environments/production.rb`
- Discarded in favor of production.jinja
- New config for silence healthcheck path commented out.
- Config for mailer commented out in jinja


### `config/environments/test.rb`
- Discarded file in favor of test.jinja
- Keep custom config for default url options
- Keep custom config for auth_adapter to mock
- Keep require integer for time.
- Keep config for show full error reports and disable caching.
- Keep config for public file server for tests with Cache-Control for performance.
- Keep config for disable caching for Action Mailer templates.
- Keep config for disable deprecation warnings.



### `config/puma.rb`
- Adds ENV["WEB_CONCURRENCY"] documentation
- Adds plugin :solid_queue if ENV["SOLID_QUEUE_IN_PUMA"]

---

## Newly Created Files

The following files were introduced as part of the Rails 8.0 upgrade.

### `db/migrate/20260422185919_add_service_name_to_active_storage_blobs.active_storage.rb`
- Discarded: Migration files already included from previous rails version

### `db/migrate/20260422185920_create_active_storage_variant_records.active_storage.rb`
- Discarded: Migration files already included from previous rails version

### `db/migrate/20260422185921_remove_not_null_on_active_storage_blobs_checksum.active_storage.rb`
- Discarded: Migration files already included from previous rails version

### `public/400.html`
- Added new file

### `public/icon.png`
- Added new file

### `public/icon.svg`
- Added new file
