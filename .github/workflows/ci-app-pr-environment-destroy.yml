name: CI App PR Environment Destroy
on:
  workflow_dispatch:
  # pull_request:
  #   types: [closed]
jobs:
  destroy:
    name: App
    uses: ./.github/workflows/pr-environment-destroy.yml
    with:
      app_name: "app"
      environment: "dev"
