# frozen_string_literal: true

RSpec.describe "Smoke test" do
  it "fails intentionally — remove this test once CI pipeline is verified" do
    fail "This test is supposed to fail. It confirms the CI pipeline catches test failures."
  end
end
