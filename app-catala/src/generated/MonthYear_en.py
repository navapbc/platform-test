# This file has been generated by the Catala compiler, do not edit!

from catala_runtime import *
from typing import Any, List, Callable, Tuple
from enum import Enum
from . import Date_internal as date_internal
from . import List_internal as list_internal
from . import List_en as list_en
from . import Date_en as date_en

class MonthYear:
    def __init__(self, year_number: Integer, month_name: date_en.Month) -> None:
        self.year_number = year_number
        self.month_name = month_name

    def __eq__(self, other: object) -> bool:
        if isinstance(other, MonthYear):
            return (self.year_number == other.year_number and self.month_name == other.month_name)
        else:
            return False

    def __ne__(self, other: object) -> bool:
        return not (self == other)

    def __str__(self) -> str:
        return "MonthYear(year_number={},month_name={})".format(self.year_number, self.month_name)


def from_date(d:Date):
    pos = (SourcePosition(filename="libcatala/monthyear_en.catala_en", start_line=18, start_column=22, end_line=18, end_column=40, law_headings=["Months of specific years"]))
    return MonthYear(year_number = date_en.get_year(d), month_name = date_en.integer_to_month(pos, date_en.get_month(d)))

def first_day_of_month(m:MonthYear):
    pos = (SourcePosition(filename="libcatala/monthyear_en.catala_en", start_line=26, start_column=5, end_line=26, end_column=24, law_headings=["Months of specific years"]))
    return date_en.of_year_month_day(pos, m.year_number, date_en.month_to_integer(m.month_name), Integer(1))

def last_day_of_month(m:MonthYear):
    pos = (SourcePosition(filename="libcatala/monthyear_en.catala_en", start_line=34, start_column=8, end_line=34, end_column=27, law_headings=["Months of specific years"]))
    return date_en.last_day_of_month(date_en.of_year_month_day(pos, m.year_number, date_en.month_to_integer(m.month_name), Integer(1)))

def is_during_or_after_month(m:MonthYear, d:Date):
    return (d >= first_day_of_month(m))

def is_before_the_month(m:MonthYear, d:Date):
    return (d < first_day_of_month(m))

def is_before_or_during_month(m:MonthYear, d:Date):
    return (d <= last_day_of_month(m))

def is_after_the_month(m:MonthYear, d:Date):
    return (d > last_day_of_month(m))

def is_in_the_month(m:MonthYear, d:Date):
    return (is_before_or_during_month(m, d) and is_during_or_after_month(m, d))
